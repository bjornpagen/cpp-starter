check_GNU_style.sh reports two classes of hits on this patch.

1. "There should be exactly one space between function name and parenthesis"
   on the existing __need_* gate lines after adding
   || defined(__need_rsize_t).
   Adjacent tokens already use defined(__need_wint_t) with no space.
   The patch matches that file. It does not match the script's
   function-call rule.

2. "Sentences should end with a dot.  Dot, space, space, end of the comment."
   on #endif /* _RSIZE_T */
   The size_t block in the same file uses #endif /* _SIZE_T */.

Do not change these lines to please the script.
They match gcc/ginclude/stddef.h.
git diff --check is clean.
